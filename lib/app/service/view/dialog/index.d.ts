import { ViewServiceMessage, Show, Push, Pop, Close, ViewService } from '../';
export { ViewServiceMessage as DialogViewServiceMEssage, Show, Push, Pop, Close };
export declare class DialogViewService extends ViewService {
}
